# Worker dependencies
#
# fastapi        — HTTP server framework (like Hono but for Python)
# uvicorn        — ASGI server that runs FastAPI (like Bun.serve for Python)
# httpx          — async HTTP client to POST alerts to the backend API
# opencv-python  — reads RTSP streams, decodes video frames into numpy arrays (full build; headless breaks ultralytics imshow patch)
# ultralytics    — YOLOv8 library (wraps PyTorch, downloads weights automatically)
# torch          — PyTorch, required by ultralytics (CPU-only)
# aiortc         — WebRTC library for Python
# aiohttp        — async HTTP (used by aiortc internally)
# numpy          — array math, required by opencv and ultralytics

fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
opencv-python==4.10.0.84
ultralytics==8.3.116
# torch and torchvision are installed as a separate Docker layer (see Dockerfile).
# For local dev outside Docker: pip install "torch>=2.6.0" "torchvision>=0.21.0"
aiortc>=1.9.0
aiohttp>=3.11.10
numpy>=1.26.4
av>=14.0.0
python-multipart==0.0.20
