# Worker dependencies
#
# fastapi        — HTTP server framework (like Hono but for Python)
# uvicorn        — ASGI server that runs FastAPI (like Bun.serve for Python)
# httpx          — async HTTP client to POST alerts to the backend API
# opencv-python  — reads RTSP streams, decodes video frames into numpy arrays
# ultralytics    — YOLOv8 library (wraps PyTorch, downloads weights automatically)
# torch          — PyTorch, required by ultralytics (CPU-only version in Docker)
# aiortc         — WebRTC library for Python (Phase 4)
# aiohttp        — async HTTP (used by aiortc internally)
# numpy          — array math, required by opencv and ultralytics

fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
opencv-python-headless==4.10.0.84
ultralytics==8.3.52
torch==2.5.1
torchvision==0.20.1
aiortc==1.9.0
aiohttp==3.11.10
numpy==1.26.4
python-multipart==0.0.20
